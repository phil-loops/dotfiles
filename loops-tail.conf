# loops-tail defaults — one source per line (see: loops-tail --list for what's live).
# server            the :3000 dev server (/tmp/loops-dev.log)
# dev:<name>        one app split out of the dev stack, e.g. dev:jobs
# svc:<name>        a loops-doctor sidecar, e.g. svc:jobs-worker, svc:mail-schedule
# preview:<name>    a loops-preview side server
# docker:<name>     a compose container, e.g. docker:clickhouse-server
server
