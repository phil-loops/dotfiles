diff --git a/scripts/viewer-solid/src/FileRail.tsx b/scripts/viewer-solid/src/FileRail.tsx
index b243d91..31e7326 100644
--- a/scripts/viewer-solid/src/FileRail.tsx
+++ b/scripts/viewer-solid/src/FileRail.tsx
@@ -334,24 +334,24 @@ export function FileEntry(props: {
       data-path={props.file.path}
       classList={{ blessed: blessed(), foil: foil() }}
     >
-      <div class="entry-head sticky top-0 z-[3] flex items-center gap-[11px] overflow-hidden border-b border-rule bg-vellum-raise px-[14px] py-[11px]">
-        <button
-          class="entry-toggle w-4 flex-none cursor-pointer p-0 text-[11px] leading-none text-ink-faint hover:text-ink"
-          title={collapsed() ? "expand" : "collapse"}
-          onClick={() => setCollapsed((c) => !c)}
-        >
+      <div
+        class="entry-head group sticky top-0 z-[3] flex cursor-pointer select-none items-center gap-[11px] overflow-hidden border-b border-rule bg-vellum-raise px-[14px] py-[11px] hover:bg-vellum-edge"
+        title={collapsed() ? "show diff" : "hide diff"}
+        onClick={(e) => {
+          if ((e.target as HTMLElement).closest("button, .sp-anchor, .chat-badge")) return;
+          setCollapsed((c) => !c);
+        }}
+      >
+        <span class="entry-toggle w-4 flex-none text-[11px] leading-none text-ink-faint group-hover:text-ink">
           {collapsed() ? "▸" : "▾"}
-        </button>
+        </span>
         <span
           class={`gutter w-3 flex-none text-center ${tarnished() ? "text-patina" : "text-gold-leaf"}`}
           classList={{ tarnish: tarnished() }}
         >
           {blessed() || foil() || tarnished() ? "✦" : ""}
         </span>
-        <span
-          class="path cursor-pointer overflow-hidden text-ellipsis whitespace-nowrap text-ink [&_.dir]:text-ink-faint [&_b]:font-semibold"
-          onClick={() => setCollapsed((c) => !c)}
-        >
+        <span class="path overflow-hidden text-ellipsis whitespace-nowrap text-ink [&_.dir]:text-ink-faint [&_b]:font-semibold">
           {seg(props.file.path)}
         </span>
         <span class="lines flex flex-none gap-2 text-[11px]">
