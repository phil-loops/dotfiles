diff --git a/scripts/viewer-solid/src/ForestMap.tsx b/scripts/viewer-solid/src/ForestMap.tsx
index d706d91..f31908d 100644
--- a/scripts/viewer-solid/src/ForestMap.tsx
+++ b/scripts/viewer-solid/src/ForestMap.tsx
@@ -811,7 +811,9 @@ export function ForestMap(props: {
                       </text>
                     )}
                   </Show>
-                  <Show when={!isGhostId(n.id) && n.description && !nhealth(n.id)?.merged}>
+                  {/* drifted hides the purpose like merged does — the ⤺ off-parent warn draws in
+                      the same sub-node slot (NODE_H/2+12 vs +13) and the two overprint */}
+                  <Show when={!isGhostId(n.id) && n.description && !nhealth(n.id)?.merged && !nhealth(n.id)?.drifted}>
                     <For each={wrapPurpose(n.description!, w)}>
                       {(line, i) => (
                         <text class={`fm-purpose ${purposeClass(n.id)}`} x={w / 2} y={NODE_H / 2 + 13 + i() * 10}>
