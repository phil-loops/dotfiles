diff --git a/scripts/stack-bless b/scripts/stack-bless
index f2a1ee2..6993f22 100755
--- a/scripts/stack-bless
+++ b/scripts/stack-bless
@@ -93,7 +93,7 @@ if (( unbless )); then
 fi
 
 # resolve parent for the file list
-[[ -n "$parent" ]] || parent=$(git config "stack-branch.${branch}.parent" 2>/dev/null || true)
+[[ -n "$parent" ]] || parent=$(git config "branch.${branch}.stack-parent" 2>/dev/null || git config "stack-branch.${branch}.parent" 2>/dev/null || true)
 [[ -n "$parent" ]] || parent=$(git config stack.main-branch 2>/dev/null || echo main)
 
 # the trunk bless-base is the REMOTE trunk (origin/main) — what the branch merges into — so a
diff --git a/scripts/stack-blessed-status b/scripts/stack-blessed-status
index 4070aec..835a516 100755
--- a/scripts/stack-blessed-status
+++ b/scripts/stack-blessed-status
@@ -30,7 +30,7 @@ while (( $# )); do
 done
 [[ -n "$branch" ]] || { echo "usage: stack-blessed-status <branch> [--parent p] [--pretty]" >&2; exit 2; }
 
-[[ -n "$parent" ]] || parent=$(git config "stack-branch.${branch}.parent" 2>/dev/null || true)
+[[ -n "$parent" ]] || parent=$(git config "branch.${branch}.stack-parent" 2>/dev/null || git config "stack-branch.${branch}.parent" 2>/dev/null || true)
 [[ -n "$parent" ]] || parent=$(git config stack.main-branch 2>/dev/null || echo main)
 
 git_dir=$(git rev-parse --path-format=absolute --git-common-dir)
diff --git a/scripts/stack-forest b/scripts/stack-forest
index 6ed281b..59765ab 100755
--- a/scripts/stack-forest
+++ b/scripts/stack-forest
@@ -101,6 +101,7 @@ def trunk_base(parent):
 
 # ---- one config read → parents / children / requires / project seeds ------
 parents, children, seeds, requires, archived = {}, {}, {}, {}, set()
+new_parents, new_requires, new_tags, old_tags = {}, {}, {}, {}
 for line in git_out(["config", "--local", "--list"]).splitlines():
     if "=" not in line:
         continue
@@ -109,19 +110,32 @@ for line in git_out(["config", "--local", "--list"]).splitlines():
     if m:
         parents[m.group(1)] = v
         continue
+    m = re.match(r"^branch\.(.+)\.stack-parent$", k)
+    if m:
+        new_parents[m.group(1)] = v
+        continue
     # fan-in deps (multivar): branches a node carries beyond its single parent
     m = re.match(r"^stack-branch\.(.+)\.requires$", k)
     if m:
         requires.setdefault(m.group(1), []).append(v)
         continue
+    m = re.match(r"^branch\.(.+)\.stack-requires$", k)
+    if m:
+        new_requires.setdefault(m.group(1), []).append(v)
+        continue
     # per-branch project tag (stack-branch.<b>.project = <name>) — the rot-free
     # successor to the stack-project.<name>.branch registry: the tag lives on the
     # branch, so deleting/renaming it can't dangle a central list. Both are read
     # during migration; a branch tagged into project N is a seed of N.
     m = re.match(r"^stack-branch\.(.+)\.project$", k)
     if m:
+        old_tags[m.group(1)] = v
         seeds.setdefault(v, []).append(m.group(1))
         continue
+    m = re.match(r"^branch\.(.+)\.stack-project$", k)
+    if m:
+        new_tags[m.group(1)] = v
+        continue
     m = re.match(r"^stack-project\.(.+)\.branch$", k)
     if m:
         seeds.setdefault(m.group(1), []).append(v)
@@ -131,6 +145,15 @@ for line in git_out(["config", "--local", "--list"]).splitlines():
     m = re.match(r"^stack-project\.(.+)\.archived$", k)
     if m and v.strip().lower() == "true":
         archived.add(m.group(1))
+parents.update(new_parents)
+requires.update(new_requires)
+for b, p in new_tags.items():
+    old = old_tags.get(b)
+    if old and old != p and b in seeds.get(old, []):
+        seeds[old].remove(b)
+        if not seeds[old]:
+            del seeds[old]
+    seeds.setdefault(p, []).append(b)
 for b, p in parents.items():
     children.setdefault(p, []).append(b)
 # a branch may appear in seeds from both the legacy registry and its per-branch
@@ -276,7 +299,8 @@ if not node_mode and target == "--standalone":
     def standalone_row(b):
         if b not in existing:        # ref deleted out from under the config → drop
             return None
-        base = git_out(["config", f"stack-branch.{b}.parent"]).strip() or MAIN
+        base = (git_out(["config", f"branch.{b}.stack-parent"]).strip()
+                or git_out(["config", f"stack-branch.{b}.parent"]).strip() or MAIN)
         n = git_out(["rev-list", "--count", f"{base}..{b}"]).strip()
         ahead = int(n) if n.isdigit() else 0
         add = dele = 0
diff --git a/scripts/stack-model b/scripts/stack-model
index 6c48a61..f2c69ac 100755
--- a/scripts/stack-model
+++ b/scripts/stack-model
@@ -16,14 +16,14 @@ here="${0:A:h}"
 
 chain=("$branch"); b="$branch"
 while :; do
-  p=$(git config "stack-branch.${b}.parent" 2>/dev/null || echo "")
+  p=$(git config "branch.${b}.stack-parent" 2>/dev/null || git config "stack-branch.${b}.parent" 2>/dev/null || echo "")
   [[ -z "$p" || "$p" == "$MAIN" ]] && break
   chain=("$p" "${chain[@]}"); b="$p"
 done
 
 links_json='[]'
 for child in "${chain[@]}"; do
-  parent=$(git config "stack-branch.${child}.parent" 2>/dev/null || echo "$MAIN")
+  parent=$(git config "branch.${child}.stack-parent" 2>/dev/null || git config "stack-branch.${child}.parent" 2>/dev/null || echo "$MAIN")
   state=$("$here/stack-blessed-status" "$child" --parent "$parent")
   files_json='[]'
   while IFS= read -r row; do
diff --git a/scripts/stack-purpose b/scripts/stack-purpose
index a485850..cc87f0c 100755
--- a/scripts/stack-purpose
+++ b/scripts/stack-purpose
@@ -45,7 +45,7 @@ if [[ -n "$desc" ]]; then
   jq -n --arg t "$desc" '{thesis:$t, enables:"", source:"description"}'; exit 0
 fi
 
-parent=$(git config "stack-branch.${branch}.parent" 2>/dev/null || true)
+parent=$(git config "branch.${branch}.stack-parent" 2>/dev/null || git config "stack-branch.${branch}.parent" 2>/dev/null || true)
 [[ -n "$parent" ]] || parent=$(git config stack.main-branch 2>/dev/null || echo main)
 git_dir=$(git rev-parse --path-format=absolute --git-common-dir)
 ledger="$git_dir/stack-purpose.json"
@@ -64,8 +64,11 @@ fi
 
 # 4) generate a suggestion (opt-in, the only token path)
 diff="$(git diff "${parent}...${branch}")"; diff="${diff[1,12000]}"
-children=$(git config --get-regexp '^stack-branch\..*\.parent$' 2>/dev/null \
-  | awk -v p="$branch" '$2==p{sub(/^stack-branch\./,"",$1); sub(/\.parent$/,"",$1); print $1}' | paste -sd', ' -)
+children=$({ git config --get-regexp '^branch\..*\.stack-parent$' 2>/dev/null \
+    | sed -E 's/^branch\.(.*)\.stack-parent /\1 /' || :
+  git config --get-regexp '^stack-branch\..*\.parent$' 2>/dev/null \
+    | sed -E 's/^stack-branch\.(.*)\.parent /\1 /' || :
+  } | awk -v p="$branch" 'NF && !seen[$1]++ && $2==p{print $1}' | paste -sd', ' -)
 
 read -r -d '' prompt <<EOF || true
 You are summarizing one branch in a stacked-PR series for a teammate skimming a dependency graph.
