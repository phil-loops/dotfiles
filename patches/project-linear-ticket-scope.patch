diff --git a/claude/forests.md b/claude/forests.md
index 76fe529..738bb07 100644
--- a/claude/forests.md
+++ b/claude/forests.md
@@ -16,7 +16,7 @@
 
 ## Config + purpose — part of the operation itself, never a follow-up
 
-At branch creation, in one motion: `git config branch.<n>.description "<thesis>"` + `stack-branch.<n>.parent` + `stack-branch.<n>.project` + `git config --add stack-project.<proj>.branch <n>`. The description is one verb-first plain line — behavior, not identifiers or terms-of-art (forest vocab is fine); it seeds the viewer and the PR-body drafter. **The description is also the commit gate**: a diff doing anything it doesn't say is another branch — a commit subject needing a different verb than the description is the "and" test failing ("move the writes into queries" landed on "moves the insert into the model", 2026-08-04). **Refresh every touched description on ANY ancestry or scope change** — a description naming a parent the branch no longer has is a bug the operation introduced. Never ask permission for any of this.
+At branch creation, in one motion: `git config branch.<n>.description "<thesis>"` + `stack-branch.<n>.parent` + `stack-branch.<n>.project` + `git config --add stack-project.<proj>.branch <n>`. The description is one verb-first plain line — behavior, not identifiers or terms-of-art (forest vocab is fine); it seeds the viewer and the PR-body drafter. **The description is also the commit gate**: a diff doing anything it doesn't say is another branch — a commit subject needing a different verb than the description is the "and" test failing ("move the writes into queries" landed on "moves the insert into the model", 2026-08-04). **Refresh every touched description on ANY ancestry or scope change** — a description naming a parent the branch no longer has is a bug the operation introduced. Never ask permission for any of this. A project born from a Linear ticket also gets, once: `git config stack-project.<proj>.ticket LOO-####` — commit scopes and the viewer's merge story then read `type(loo-####):` instead of `type(<proj>):`.
 
 The `stack-*` keys rot (git manages only the `branch.<n>.*` namespace — deletes/renames leave dangling `stack-*` entries). Rename checklist: `git branch -m`, migrate `.parent` + `.project`, fix the registry entry, re-point children's `.parent`. Health-scan: `stack-doctor [<project>]`; sweep dead config: `stack-doctor --prune` (`--dry-run` previews).
 
diff --git a/scripts/stack-squash b/scripts/stack-squash
index 7538816..8682222 100755
--- a/scripts/stack-squash
+++ b/scripts/stack-squash
@@ -315,17 +315,22 @@ diffstat = git(["diff", "--stat", f"{base}...{branch}"]).stdout.strip()
 patch = git(["diff", f"{base}...{branch}"]).stdout
 patch_clip = "\n".join(patch.splitlines()[:300])   # keep the prompt bounded
 
-# scope comes from the forest project, not the code area the voice corpus (main) teaches
+# scope comes from the forest project — or its Linear ticket when one is tied
+# (stack-project.<p>.ticket) — not the code area the voice corpus (main) teaches
 project = (git(["config", f"branch.{branch}.stack-project"]).stdout.strip()
            or git(["config", f"stack-branch.{branch}.project"]).stdout.strip())
+ticket = (git(["config", f"stack-project.{project}.ticket"]).stdout.strip().lower()
+          if project else "")
+scope = ticket or project
 
 _SUBJECT_RE = re.compile(r"^(?P<type>[a-z]+)(?:\((?P<scope>[^)]*)\))?(?P<bang>!?):\s*(?P<rest>.+)$")
 
 
 def force_scope(msg):
-    """Pin the subject's conventional-commit scope to the stack project. Saying it in the
-    prompt isn't enough: the drafter studies main, where scope IS a code area, and drifts."""
-    if not (project and msg):
+    """Pin the subject's conventional-commit scope to the stack project (or its Linear
+    ticket). Saying it in the prompt isn't enough: the drafter studies main, where scope
+    IS a code area, and drifts."""
+    if not (scope and msg):
         return msg
     lines = msg.splitlines()
     m = _SUBJECT_RE.match(lines[0].strip())
@@ -339,15 +344,16 @@ def force_scope(msg):
                   if cm), "")
         if not t:
             return msg
-        lines[0] = f"{t}({project}): {lines[0].strip()}"
+        lines[0] = f"{t}({scope}): {lines[0].strip()}"
         return "\n".join(lines)
-    lines[0] = f"{m['type']}({project}){m['bang']}: {m['rest'].strip()}"
+    lines[0] = f"{m['type']}({scope}){m['bang']}: {m['rest'].strip()}"
     return "\n".join(lines)
 
 
 scope_instr = (
-    f"\nThe conventional-commit scope MUST be exactly `{project}`: this branch belongs to the\n"
-    f"`{project}` stack project, and every branch in a forest carries the project as its scope so\n"
+    f"\nThe conventional-commit scope MUST be exactly `{scope}`: this branch belongs to the\n"
+    f"`{project}` stack project" + (f" (Linear ticket `{ticket}`)" if ticket else "")
+    + ", and every branch in a forest carries the same scope so\n"
     f"the forest is legible from `git log` alone. The code-area scopes in the voice examples are\n"
     f"main's convention, NOT this forest's — do not copy them.\n"
 ) if project else ""
