diff --git a/scripts/stack-forest b/scripts/stack-forest
index c78058f..281042d 100755
--- a/scripts/stack-forest
+++ b/scripts/stack-forest
@@ -376,9 +376,20 @@ def _scratch_branch(path):
     # stack-open's scratch worktrees are DETACHED (so they never lock the branch) and record
     # their branch in a stack-open-branch marker; without it their working-tree edits — what
     # open-in-nvim writes — are invisible to the uncommitted rail.
-    gd = git_out(["-C", path, "rev-parse", "--absolute-git-dir"]).strip()
+    # Resolved via the worktree's .git gitdir pointer, pure filesystem — a git subprocess per
+    # detached worktree made every /node fetch O(worktree count), ~0.5s across 40 scratch dirs.
     try:
-        return open(os.path.join(gd, "stack-open-branch")).read().strip() or None if gd else None
+        with open(os.path.join(path, ".git")) as fh:   # main worktree: .git is a dir → OSError → None
+            pointer = fh.read().strip()
+    except OSError:
+        return None
+    if not pointer.startswith("gitdir:"):
+        return None
+    gd = pointer[len("gitdir:"):].strip()
+    if not os.path.isabs(gd):
+        gd = os.path.normpath(os.path.join(path, gd))
+    try:
+        return open(os.path.join(gd, "stack-open-branch")).read().strip() or None
     except OSError:
         return None
 
