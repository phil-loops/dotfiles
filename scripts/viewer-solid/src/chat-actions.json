{
  "_doc": "The ONE registry of actions the read-only ✦ chat may offer as buttons. Both sides read this file: srv/prompts.py teaches Claude the menu from it, and src/chatActions.ts dispatches a clicked button from it — so the taught vocabulary and the wired dispatch can never drift. The chat itself stays read-only (Read/Grep/Glob); an action is a PROPOSAL the viewer renders and the human clicks to run against an endpoint that already exists. The editing actions (apply-change/add-file/reforest) all POST /claude — they hand a task the chat composed to a full editing Claude in the branch's worktree, which is how a read-only suggestion turns into real work.",
  "actions": [
    {
      "id": "whats-next",
      "endpoint": "/next",
      "scope": "branch",
      "confirm": false,
      "label": "What merges next?",
      "describe": "Read-only navigation: returns the LIVE, server-computed merge-readiness of this branch's forest — which branches have an open PR vs are clear to merge into main right now, in canonical order. This is state you CANNOT infer from the diff (it checks real PRs), so offer it whenever the user asks what comes next / which branch lands next / where this sits in the merge order — even if you can sketch the structure yourself, the button adds the current PR + mergeable status you don't have. params: none.",
      "params": [],
      "body": { "branch": "$branch" }
    },
    {
      "id": "catch-me-up",
      "endpoint": "/chat",
      "scope": "branch",
      "confirm": false,
      "label": "Catch me up",
      "describe": "Read-only recall: summarize where this branch sits — its purpose, what parent..branch changes, what's still unblessed, and its place in the forest. Offer when the user asks 'remind me what this is' or 'where were we'. It sends itself as a normal chat message (reuses the ✦ chat), so the answer streams below. params: none.",
      "params": [],
      "message": "Catch me up on this branch: its purpose, what it changes versus its parent, what's still unblessed, and where it sits in the forest."
    },
    {
      "id": "apply-change",
      "endpoint": "/claude",
      "scope": "branch",
      "confirm": true,
      "label": "Apply this change",
      "describe": "Hand a specific edit to an editing Claude in the branch's worktree — this is how your suggestion actually gets made. Offer whenever you've described a concrete change to an existing file. params: file (the file to edit; in a file chat, the file you're viewing), instruction (the exact change to make).",
      "params": [
        { "name": "file", "desc": "path of the file to edit" },
        { "name": "instruction", "desc": "a precise, self-contained instruction describing the edit" }
      ],
      "body": { "branch": "$branch", "selections": [{ "path": "$file" }], "instruction": "$instruction" }
    },
    {
      "id": "add-file",
      "endpoint": "/claude",
      "scope": "branch",
      "confirm": true,
      "label": "Add this file",
      "describe": "Create a NEW file via an editing Claude in the worktree — most often a missing test. Offer when the change lacks a file it clearly should have. params: file (the new file's path), instruction (what it should contain).",
      "params": [
        { "name": "file", "desc": "path of the new file to create" },
        { "name": "instruction", "desc": "what the new file should contain (e.g. the test cases to write, and for what)" }
      ],
      "body": { "branch": "$branch", "selections": [{ "path": "$file" }], "instruction": "$instruction" }
    },
    {
      "id": "reforest",
      "endpoint": "/claude",
      "scope": "branch",
      "confirm": true,
      "label": "Reforest this branch",
      "describe": "Hand the whole branch to an editing Claude to split it into a reviewable stacked forest — single-capability branches, each with its parent/purpose set, fanning in where an integrator needs several bases (the repo's forest conventions, which that Claude reads). Offer when the branch bundles several independent capabilities into one fat change. params: instruction (how to split it — name the capabilities you'd separate).",
      "params": [
        { "name": "instruction", "desc": "how to split the branch into a forest — the capabilities to separate, in plain language" }
      ],
      "body": { "branch": "$branch", "selections": [{ "path": "." }], "instruction": "$instruction" }
    },
    {
      "id": "set-purpose",
      "endpoint": "/purpose",
      "scope": "branch",
      "confirm": false,
      "label": "Set branch purpose",
      "describe": "Save a one-line purpose as this branch's git description. Offer ONLY when the branch appears to have no purpose yet, or the user is explicitly asking about the branch's intent — never as a generic follow-up to an unrelated answer. params: text (the one-line purpose in plain language — what the branch does, not how it's coded).",
      "params": [{ "name": "text", "desc": "the one-line purpose in plain language — what the branch does, not how it's coded" }],
      "body": { "branch": "$branch", "text": "$text" }
    },
    {
      "id": "bless-file",
      "endpoint": "/bless",
      "scope": "file",
      "confirm": false,
      "label": "Bless this file",
      "describe": "Mark the file under review as blessed. Offer only in a file-scoped chat when the file's change looks correct and complete.",
      "params": [],
      "body": { "branch": "$branch", "file": "$path" }
    },
    {
      "id": "squash",
      "endpoint": "/squash",
      "scope": "branch",
      "confirm": true,
      "label": "Squash to working tree",
      "describe": "Collapse the branch's parent..branch history into unstaged working-tree changes (Phil writes the commit himself). Offer when the branch has messy WIP commits worth flattening.",
      "params": [],
      "body": { "branch": "$branch" }
    },
    {
      "id": "prep-to-merge",
      "endpoint": "/prep",
      "scope": "branch",
      "confirm": true,
      "label": "Prep to merge",
      "describe": "Squash the whole branch into one clean, formatted commit for a single-commit PR. Offer when the branch is ready to ship.",
      "params": [],
      "body": { "branch": "$branch" }
    },
    {
      "id": "restack",
      "endpoint": "/sync",
      "scope": "branch",
      "confirm": true,
      "label": "Restack onto origin/main",
      "describe": "Rebase this unpublished root branch onto fresh origin/main. Offer when the branch has fallen behind main.",
      "params": [],
      "body": { "branch": "$branch" }
    }
  ]
}
